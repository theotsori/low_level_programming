#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers
 * @a: int add b
 * @b: int add a
 * Return: the results
 */
int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
