#include "main.h"
/**
 * _islower - checks for lowercase
 *
 * description - is lower checks for lower case characters
 *
 * @c: int character
 *
 * Returns: 1 when c lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
