#include "main.h"
/**
 * main - _islower checks for lowercase letters
 *
 * _islower - lowercase
 *
 * @c: int character
 *
 * Returns: 1 when c lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
