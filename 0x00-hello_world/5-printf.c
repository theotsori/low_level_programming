#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Exits with 0 when everything is fine
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
