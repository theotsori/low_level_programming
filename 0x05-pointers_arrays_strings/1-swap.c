#include "main.h"

/**
 * swap_int - function that swaps values of two integers
 * @a: one of the int
 * @b: one of the int
 *
 * Return: 0
 */
void swap_int(int *a, int *b)
{
	*a = INT_MAX;
	*b = INT_MIN;
}
