#include "main.h"
/**
 * _islower - checks for lowercase characters
 *
 * Description: _islower checks for lower case characters
 *
 * @c: int character
 *
 * Return: 1 when c lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
